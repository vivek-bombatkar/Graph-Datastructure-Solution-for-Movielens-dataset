from setuptools import setup

setup(
   name='ml_graph',
   version='1.0',
   description='A ml graph module',
   author='vivek',
   author_email='',
   packages=['ml_graph'],  
   install_requires=['none'], 
)
